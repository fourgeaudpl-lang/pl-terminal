# Variables d'environnement optionnelles (surchargent config.yaml).
# Copie en .env et adapte, puis charge-les avant de lancer le bot.

# URL du terminal déployé (spreads + calendrier).
TERMINAL_BASE_URL=

# Webhook d'alertes (Discord / Slack). Laisse vide pour la console seule.
WEBHOOK_URL=
